apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  annotations:
    # Overwritten by templates
    serving.kserve.io/enable-prometheus-scraping: "true"
    serving.kserve.io/deploymentMode: {{ deployment_mode | default("RawDeployment") }}
  labels:
    opendatahub.io/dashboard: "true"
  name: {{ model_name | default("ministral-8b-instruct-2410") }}
spec:
  predictor:
    minReplicas: {{ min_replica | default(1) }}
    serviceAccountName: modelmesh-serving-sa
    model:
      modelFormat:
        name: {{ model_format | default("pytorch") }}
      runtime: {{ runtime_name | default("vllm-runtime") }}
      storageUri: {{ storage_uri | default("s3://ods-ci-wisdom/mistralai/Ministral-8B-Instruct-2410/") }}
      args:
        - --dtype=bfloat16
        - --model=/mnt/models
        - --uvicorn-log-level=debug
        - --max-model-len=2048
        - --served-model-name={{ model_name | default("ministral-8b-instruct-2410") }}
        {% for arg in new_args %}
        - {{ arg }}
        {% endfor %}
      env:
        - name: HF_HUB_CACHE
          value: /tmp
        {% for var in env_vars %}
        - name: {{ var.name }}
          value: {{ var.value }}
        {% endfor %}
      resources:
        requests:
          cpu: "8"
          memory: "14Gi"
          
        limits:
          memory: "32Gi"
      volumeMounts:
        {% if gpu_count | default(0) | int >= 2 %}
        - name: shared-memory
          mountPath: /dev/shm
        - name: tmp
          mountPath: /tmp
        - name: home
          mountPath: /home/vllm
        {% endif %}
    volumes:
      {% if gpu_count | default(0) | int >= 2 %}
      - name: shared-memory
        emptyDir:
          medium: Memory
          sizeLimit: 16Gi
      - name: tmp
        emptyDir: {}
      - name: home
        emptyDir: {}
      {% endif %}
